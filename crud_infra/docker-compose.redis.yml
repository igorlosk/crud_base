version: '3.9'

services:
  redis:
    image: redis:7
    container_name: crud_base-redis
    ports:
      - "6379:6379"
    volumes:
      - crud_base_redis_data:/data

volumes:
  crud_base_redis_data:
